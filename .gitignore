# Dependencies and build output — never edit these by hand
node_modules/
dist/
.astro/

# Private / source files kept OFF GitHub on purpose:
# - the CV PDF contains your phone number (do not publish)
# - the original photo/cover files; the website only needs the smaller
#   processed copies in public/ (headshot.jpg, book-cover.jpg)
SergeyVoelkeCVJune.pdf
file_000000001b3871f4b6335aea59d8dda6(1).png
book cover.jpg

# Internal working notes — kept local, not published:
# - claude.md: personal instructions/preferences for the AI assistant
# - SPEC.md: internal build spec with decisions and placeholders
claude.md
SPEC.md

# Environment variables with private details (Impressum address, email).
# Real values live here locally and in the Cloudflare dashboard — never in git.
# .env.example (blank template) IS committed, as documentation.
.env
.env.*
!.env.example

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
